[tox]
envlist = py-tornado

[testenv]
passenv = *
changedir = {toxworkdir}
deps=
  selenium
  parameterized
  syncer
  coverage
commands=
  coverage run --source wdom {toxinidir}/tests/run_tests.py

[flake8]
exclude=wdom/themes/*,docs/*,tmp/*
