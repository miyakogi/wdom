[tox]
envlist = py-tornado

[testenv]
passenv = *
changedir = {toxworkdir}
deps=
  selenium
  parameterized
  syncer
commands=
  python ${toxinidir}/tests/run_tests

[flake8]
exclude=wdom/themes/*,docs/*,tmp/*
